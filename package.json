{
  "name": "@app/monorepo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "migrate:dev": "npm run migrate:dev -w packages/db",
    "migrate:reset": "npm run migrate:reset -w packages/db",
    "typecheck": "turbo run typecheck",
    "validate": "turbo run lint typecheck && turbo run build"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "@eslint/js": "^9.10.0",
    "eslint": "^9.10.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.2.1",
    "globals": "^15.9.0",
    "prettier": "^3.3.3",
    "ty": "^0.0.1",
    "typescript-eslint": "^8.4.0"
  },
  "engines": {
    "node": ">=20.11.0"
  }
}
